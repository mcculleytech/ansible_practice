---

- hosts: all
  become: true
  tasks:
  

  - name: install apache2 package and add php support for apache.
    package:
      name: 
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes
